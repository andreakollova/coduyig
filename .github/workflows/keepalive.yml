name: Keep Alive

on:
  schedule:
    - cron: '0 5 * * *'  # 05:00 UTC daily — runs before all other workflows

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Keepalive ping
        run: echo "Keepalive $(date -u)"
